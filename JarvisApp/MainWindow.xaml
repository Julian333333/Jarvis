<Window
    x:Class="JarvisApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <Grid Grid.Row="0" Background="{ThemeResource SystemAccentColor}" Padding="16,12">
            <TextBlock Text="JARVIS AI Assistant" 
                      Style="{StaticResource TitleTextBlockStyle}"
                      Foreground="White"/>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="1" Padding="24">
            <StackPanel Spacing="20" MaxWidth="800" HorizontalAlignment="Center" VerticalAlignment="Center">
                
                <!-- Welcome Card -->
                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                       BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                       BorderThickness="1"
                       CornerRadius="8"
                       Padding="24">
                    <StackPanel Spacing="12">
                        <TextBlock Text="Welcome to JARVIS" 
                                  Style="{StaticResource SubtitleTextBlockStyle}"/>
                        <TextBlock Text="Your intelligent AI assistant powered by Windows 11" 
                                  Style="{StaticResource BodyTextBlockStyle}"
                                  Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                  TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <!-- Input Section -->
                <StackPanel Spacing="12">
                    <TextBox x:Name="InputTextBox"
                            PlaceholderText="Ask me anything..."
                            Height="100"
                            TextWrapping="Wrap"
                            AcceptsReturn="True"/>
                    
                    <Button Content="Send" 
                           HorizontalAlignment="Right"
                           Style="{StaticResource AccentButtonStyle}"
                           Click="SendButton_Click"/>
                </StackPanel>

                <!-- Response Section -->
                <Border Background="{ThemeResource LayerFillColorDefaultBrush}"
                       BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                       BorderThickness="1"
                       CornerRadius="8"
                       Padding="24"
                       MinHeight="200">
                    <ScrollViewer>
                        <TextBlock x:Name="ResponseTextBlock"
                                  Text="Response will appear here..."
                                  Style="{StaticResource BodyTextBlockStyle}"
                                  Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                  TextWrapping="Wrap"/>
                    </ScrollViewer>
                </Border>

                <!-- Status Bar -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Orientation="Horizontal" Spacing="8" Grid.Column="0">
                        <FontIcon Glyph="&#xE72C;" FontSize="16"/>
                        <TextBlock x:Name="StatusTextBlock" 
                                  Text="Ready" 
                                  VerticalAlignment="Center"
                                  Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>
                    
                    <TextBlock Text="WinUI 3 • .NET 8" 
                              Grid.Column="2"
                              Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                              VerticalAlignment="Center"/>
                </Grid>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
